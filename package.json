{
  "name": "custom-scroll-behaviors",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./target && tsc --declarationMap && cp README.md target/ && cp release.package.json target/package.json",
    "start": "npm run build"
  },
  "author": "",
  "license": "MPL 2.0",
  "dependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3"
  }
}
